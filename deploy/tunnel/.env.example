# Copy to .env, fill in, and make it readable by you alone:
#
#   cp .env.example .env && chmod 600 .env
#
# This one file holds everything, credentials included. docs/deploy/tunnel.md
# walks through where each value comes from. Every setting the server takes is
# documented in .env.example at the repo root; anything from there can be added
# here too.

# --- The deploy ---
# The image tag to run: a release, never latest, so an update is a decision.
GOSTORE_VERSION=v1.0.0
# The tunnel's connector token, from the Cloudflare dashboard when you create
# the tunnel. It is the tunnel's whole credential.
TUNNEL_TOKEN=
# The two public hostnames you give the tunnel: the store, routed to
# http://server:8080, and product images, routed to http://minio:9000.
# BASE_URL and BLOB_PUBLIC_BASE_URL are derived from them.
DOMAIN=shop.example.com
IMAGES_DOMAIN=images.example.com

# --- The store ---
STORE_NAME=Your Store
CURRENCY=ZAR

# --- Generated secrets ---
# Nobody chooses these. Hex, so they can sit inside a URL unescaped:
#   openssl rand -hex 32
POSTGRES_PASSWORD=
MINIO_ROOT_PASSWORD=

# --- PayFast ---
# true until the three values below are your live ones. Required — there is no
# default here, on purpose.
PAYFAST_SANDBOX=true
# PayFast's published sandbox credentials. Replace all three before going live.
PAYFAST_MERCHANT_ID=10000100
PAYFAST_MERCHANT_KEY=46f0cd694581a
PAYFAST_PASSPHRASE=jt7NOE43FZPn

# --- Mail: Microsoft Graph ---
# Required: a digital download's link exists only in its confirmation email.
# An Entra app registration with the Mail.Send application permission,
# admin-consented — see "Microsoft Exchange Online" in docs/email.md. Not the
# same permission as SMTP's SMTP.SendAsApp; the two are not interchangeable.
GRAPH_TENANT_ID=
GRAPH_CLIENT_ID=
GRAPH_CLIENT_SECRET=
# The mailbox the store sends as.
EMAIL_FROM=orders@example.com
# Optional: who gets a copy of each paid order.
ORDER_NOTIFY_EMAIL=

# --- Purchased files (digital products) ---
# A volume on this server, streamed through the store. Delete the line to sell
# no downloads.
DOWNLOAD_DIR=/downloads

# --- Optional ---
# Supply the first-admin setup token instead of reading a generated one from
# the logs. 32 characters minimum.
# SETUP_TOKEN=
